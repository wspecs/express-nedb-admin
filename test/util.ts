import * as lib from '../lib/utils';
import { expect } from 'chai';

describe('util file', () => {
  beforeEach(() => {
    // Add before each test
  });

  it('test place holder', () => {
    expect(7).to.equal(7);
  });
});
